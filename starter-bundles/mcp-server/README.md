# MCP Server Starter Bundle

Edit these files before enabling the services:

- `config.py`
- `deploy/mcp-server.service`
- `deploy/mcpo.service`

Only replace the placeholder values inside angle brackets.
